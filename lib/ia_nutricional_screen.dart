import 'package:flutter/material.dart';
class IANutricionalScreen extends StatelessWidget {
  const IANutricionalScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Pantalla de IA Nutricional')));
  }
}